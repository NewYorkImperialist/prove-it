// Tailwind v4 runs as a single PostCSS plugin; all theme configuration lives in
// app/globals.css (@theme), so there is no tailwind.config.js.
export default { plugins: { "@tailwindcss/postcss": {} } };
